{
  "name": "deep-freeze-node2",
  "version": "1.1.4",
  "description": "Recursively Object.freeze() objects [updated].",
  "keywords": [
    "deep",
    "recursive",
    "object",
    "array",
    "properties",
    "freeze",
    "node"
  ],
  "homepage": "https://github.com/madxor/deep-freeze-node2",
  "bugs": {
    "url": "https://github.com/madxor/deep-freeze-node2/issues",
    "email": "anatoliy.gatt@aol.com"
  },
  "license": "MIT",
  "author": {
    "name": "Anatoliy Gatt",
    "email": "anatoliy.gatt@aol.com",
    "url": "http://anatoliygatt.com"
  },
  "contributors": [],
  "files": [
    "LICENSE",
    "CHANGELOG.md",
    "README.md",
    "index.js",
    "lib/"
  ],
  "main": "index.js",
  "bin": {},
  "man": {},
  "directories": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/madxor/deep-freeze-node2.git"
  },
  "scripts": {
    "test": "mocha --colors --sort --bail --reporter spec --check-leaks --full-trace --no-timeouts --recursive ./test/*.js",
    "test-ci": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --colors --sort --bail --reporter spec --check-leaks --full-trace --no-timeouts --recursive ./test/*.js",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --colors --sort --bail --reporter dot --check-leaks --full-trace --no-timeouts --recursive ./test/*.js",
    "test-tap": "mocha --colors --sort --bail --reporter tap --check-leaks --full-trace --no-timeouts --recursive ./test/*.js"
  },
  "config": {},
  "devDependencies": {
    "codeclimate-test-reporter": "^0.5.0",
    "istanbul": "^0.4.5",
    "mocha": "^8.3.0"
  },
  "peerDependencies": {},
  "bundledDependencies": [],
  "engines": {
    "node": ">= 6.0.0",
    "npm": ">= 3.0.0"
  },
  "preferGlobal": false,
  "private": false,
  "publishConfig": {}
}
